name = "workflows-starter-template"
main = "src/index.js"
compatibility_date = "2026-08-01"

[triggers]
# Fires every 14 minutes.
# Note: Cron fields are in UTC; also, some schedulers can be interpreted in a way that
# may require testing to ensure exact cadence matches your expectation.
crons = ["*/14 * * * *"]